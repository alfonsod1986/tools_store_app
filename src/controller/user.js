'use strict';

const db = require('../config/db.config');
const User = db.users;

const controller = {};

/**
 * Log in
 * 
 * @param req
 * @param res
 * 
 * @returns user
 */
controller.logIn = (req, res) => {};

/* Export module */
module.exports = controller;